name: Sync Fork

on:
  schedule:
    - cron: '* */6 * * *' # everyday
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.8
        with:
          owner: gaotianliuyun
          base: master
          head: master
